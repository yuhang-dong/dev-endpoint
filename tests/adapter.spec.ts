import { test, expect } from "@playwright/test";
test.describe("Playwright Test", () => {
  test.beforeEach(async () => {
    
  });

  test.afterEach(async () => {
  });

  test("should support custom adapter", async () => {
  

    expect(1).toEqual(1);
  });
});
